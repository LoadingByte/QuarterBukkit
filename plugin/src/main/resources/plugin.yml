name: ${project.name}
version: ${project.version}
description: ${project.description}
prefix: ${project.name}
authors: [QuarterCode, LoadingByte, marcel747800]
website: ${project.url}
main: ${main.class}
load: startup
